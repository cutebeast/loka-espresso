# Database
DB_PASSWORD=change-me-to-a-strong-password
DATABASE_URL=postgresql+asyncpg://fnb:change-me-to-a-strong-password@db:5432/fnb

# JWT Authentication
JWT_SECRET=change-me-to-a-random-64-char-string
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=30

# CORS (comma-separated origins)
CORS_ORIGINS=https://app.loyaltysystem.uk,https://admin.loyaltysystem.uk

# File uploads
UPLOAD_DIR=/app/uploads

# Webhooks
WEBHOOK_API_KEY=change-me-to-a-secure-api-key
WEBHOOK_SIGNING_SECRET=change-me-to-a-webhook-signing-secret

# OTP bypass (ONLY enable in development, NEVER in production)
OTP_BYPASS_ALLOWED=false
