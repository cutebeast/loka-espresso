# Admin Portal Environment
# Local dev: full URL to backend (no reverse proxy between frontend and backend)
NEXT_PUBLIC_API_URL=http://localhost:13800/api/v1
# Container: use /api/v1 (Caddy reverse proxy handles API routing)
# NEXT_PUBLIC_API_URL=/api/v1
PORT=3000
HOST=0.0.0.0
