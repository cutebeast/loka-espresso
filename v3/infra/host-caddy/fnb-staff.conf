# F&B Staff Portal v3 — {$STAFF_DOMAIN:staff.loyaltysystem.uk}
https://{$STAFF_DOMAIN:staff.loyaltysystem.uk} {
    tls {$TLS_CERT_PATH:/etc/ssl/certs/loyaltysystem-origin.pem} {$TLS_KEY_PATH:/etc/ssl/private/loyaltysystem-origin.key} {
        protocols tls1.2
    }

    # CORS for version.json
    handle /version.json {
        header Access-Control-Allow-Origin *
        header Access-Control-Allow-Methods "GET, OPTIONS"
        header Access-Control-Allow-Headers "Content-Type"
        root * /root/fnb-super-app/v3/staff-portal/public
        file_server
    }

    # API proxy → v3 backend
    handle /api/* {
        reverse_proxy localhost:13800 {
            header_up Host localhost
        }
    }

    # Health check
    handle /health {
        reverse_proxy localhost:13800 {
            header_up Host localhost
        }
    }

    # Uploads — proxy to v3 backend so the backend StaticFiles mount serves them
    handle /uploads/* {
        reverse_proxy localhost:13800 {
            header_up Host localhost
        }
    }

    # Everything else → v3 staff portal (Next.js on port 13820)
    handle {
        reverse_proxy localhost:13820
    }

    log {
        output file /var/log/caddy/fnb-staff.log
    }
}

# HTTP — proxy directly (Cloudflare Flexible SSL)
http://{$STAFF_DOMAIN:staff.loyaltysystem.uk} {
    handle /api/* {
        reverse_proxy localhost:13800 {
            header_up Host localhost
        }
    }
    handle /health {
        reverse_proxy localhost:13800 {
            header_up Host localhost
        }
    }
    handle /uploads/* {
        reverse_proxy localhost:13800 {
            header_up Host localhost
        }
    }
    handle {
        reverse_proxy localhost:13820
    }
}
