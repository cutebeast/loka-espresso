# Staff Portal Environment
# Server-side absolute URL to the backend (used by auth route handlers/middleware)
API_PROXY_URL=http://127.0.0.1:13800/api
# Public-facing API path (browser uses same-origin /api when behind Caddy)
NEXT_PUBLIC_API_URL=/api
PORT=3000
HOST=0.0.0.0
